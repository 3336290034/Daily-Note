/* 遇到的问题 */
1， Linux（Ubuntu）下的[根目录]和[用户主目录]注意区分！
    ~ 或 ~/：用户主目录（等价于 /home/用户名）               cd ~  进入用户主目录
    /：根目录，Linux 系统的 “绝对起点”                       cd /  进入根目录
    ./：当前目录（例如在主目录中，./文档 等价于 ~/文档）

2， vim编辑器的使用：
    1. 底行模式： 

3， 老生常谈的问题：“结构体在定义的时候包含自己？”  [实则不然] https://www.doubao.com/thread/w28075ddab79fa486  && 那我问你，递归函数自己包含自己怎么回事呢？
    ** 指针的大小是固定的！！！
4， 还是那个熟悉的 malloc 函数      原型： void *malloc(size_t size)；  头文件 <stdlib.h>
        ** 典型的动态分配内存函数 **

5， How about your Comprehension for Pointer？！
    1. 指针也是变量：大小是固定的，只是各种架构下的大小可能有差异，一般是4或8字节；
    2. 指针变量存的是指向地址，这点注意下，这就注定了他和数组名不是一路的，
        数组名 有且仅 表示一片连续内存空间的首地址

6， 4KB边界对齐（地址对齐）的哇啦哇啦………………？
    https://www.doubao.com/thread/w103cd35b78b5823b 虽有些微浪费，但是保证了硬件的访问速度~
	
7， Linux在运行 可执行目标文件之前（如果需要动态链接库），需要将对应的 .so 文件放到 /lib、 /usr/lib 等系统默认路径下

补充5.12的：
	进程的虚拟空间 = 用户空间（独有的） + 内核空间（共享的）
	 ** 后面的“内核空间（共享的）”是指这部分的虚拟地址都会通过各自的页表映射到同样的物理地址吗？  --- Yes.